name: Temporary WireGuard VPN Server (Pinggy UDP Fix)
on:
  workflow_dispatch:

jobs:
  deploy-vpn-server:
    runs-on: ubuntu-latest
    steps:
      - name: Install required dependencies
        run: |
          sudo apt update && sudo apt install -y wireguard jq socat openssh-client

      - name: Generate WireGuard key pair and config
        run: |
          sudo mkdir -p /etc/wireguard
          wg genkey | sudo tee /etc/wireguard/server_private.key | wg pubkey | sudo tee /etc/wireguard/server_public.key
          wg genkey | sudo tee /etc/wireguard/client_private.key | wg pubkey | sudo tee /etc/wireguard/client_public.key
          
          SERVER_PRIV=$(sudo cat /etc/wireguard/server_private.key)
          SERVER_PUB=$(sudo cat /etc/wireguard/server_public.key)
          CLIENT_PRIV=$(sudo cat /etc/wireguard/client_private.key)
          CLIENT_PUB=$(sudo cat /etc/wireguard/client_public.key)
          
          DEFAULT_IFACE=$(ip route get 1.1.1.1 | awk '{print $5; exit}')
          
          sudo tee /etc/wireguard/wg0.conf > /dev/null <<EOF
          [Interface]
          PrivateKey = $SERVER_PRIV
          Address = 10.0.0.1/24
          ListenPort = 1194
          PostUp = iptables -A FORWARD -i wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o $DEFAULT_IFACE -j MASQUERADE; ufw allow 1194/udp
          PostDown = iptables -D FORWARD -i wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o $DEFAULT_IFACE -j MASQUERADE; ufw delete allow 1194/udp
          [Peer]
          PublicKey = $CLIENT_PUB
          AllowedIPs = 10.0.0.2/32
          PersistentKeepalive = 25
          EOF
          
          tee client.conf > /dev/null <<EOF
          [Interface]
          PrivateKey = $CLIENT_PRIV
          Address = 10.0.0.2/24
          DNS = 8.8.8.8
          [Peer]
          PublicKey = $SERVER_PUB
          Endpoint = PINGGY_ADDR
          AllowedIPs = 0.0.0.0/0
          PersistentKeepalive = 25
          EOF

      - name: Start WireGuard service & fix network forward
        run: |
          # 强制开启 IP 转发并持久化
          sudo sysctl -w net.ipv4.ip_forward=1
          echo "net.ipv4.ip_forward=1" | sudo tee -a /etc/sysctl.conf
          sudo sysctl -p
          
          # 关闭并清空防火墙规则（兜底）
          sudo ufw disable
          sudo iptables -F
          sudo iptables -t nat -F
          
          # 启动 WireGuard 并验证状态
          sudo wg-quick up wg0
          sudo wg show wg0
          echo "=== 网卡转发状态 ==="
          cat /proc/sys/net/ipv4/ip_forward
          echo "=== iptables NAT 规则 ==="
          sudo iptables -t nat -L POSTROUTING -n

      - name: Expose VPN port via Pinggy free UDP tunnel
        run: |
          mkdir -p ~/.ssh
          echo "Host a.pinggy.io" >> ~/.ssh/config
          echo "  StrictHostKeyChecking no" >> ~/.ssh/config
          echo "  UserKnownHostsFile /dev/null" >> ~/.ssh/config
          
          # 启动隧道并输出实时日志
          nohup ssh -T -p 443 -R0:localhost:1194 udp@a.pinggy.io > pinggy.log 2>&1 &
          sleep 25
          
          # 双重验证 Pinggy 地址
          PINGGY_ADDR=$(grep -o 'udp://.*:[0-9]*' pinggy.log | head -n1 | cut -d'/' -f3)
          if [ -z "$PINGGY_ADDR" ]; then
              echo "=== Pinggy 启动失败，日志内容 ==="
              cat pinggy.log
              exit 1
          fi
          echo "✅ Pinggy 公网地址: $PINGGY_ADDR"
          
          sed -i "s/PINGGY_ADDR/$PINGGY_ADDR/g" client.conf
          
          echo -e "\n===== WireGuard 客户端配置开始 ====="
          cat client.conf
          echo -e "===== WireGuard 客户端配置结束 =====\n"

      - name: Monitor WireGuard handshake (关键修复)
        run: |
          echo "=== 等待客户端连接，实时监控握手状态 ==="
          while true; do
              sudo wg show wg0 latest-handshakes
              sleep 10
          done &
          sleep 35000
